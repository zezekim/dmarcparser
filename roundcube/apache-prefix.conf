# Serve Roundcube under /roundcube as well as / so the reverse proxy
# (mxsentinel Caddy) can pass the path through without stripping it.
# Roundcube then sees the real request path and generates correct
# redirects and asset URLs.
Alias /roundcube /var/www/html
